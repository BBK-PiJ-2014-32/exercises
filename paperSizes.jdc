int width = 26;
int height = 37;

int size = 0;

getPaperSize(size, width);
double result = getPaperSize(size, width);
println(result);


//Recursive method to return size as string

double getPaperSize(int x, double y){
		int i = 10 ;
		if (x <= i )	{
			double height = getPaperSize((x+1), y) * Math.sqrt(2);
			return height;
			}
		return 37;	
	}


class paperSize {
	int size;
	int width;
	int height;
	}
	